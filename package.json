{
  "name": "parcel-plugin-web-ext-tool",
  "version": "0.0.1",
  "description": "A parcel plugin for web-ext.",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mmktomato",
  "license": "MIT",
  "peerDependencies": {
    "parcel-bundler": ">=1.x",
    "web-ext": ">=4.x"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "web-ext": "^4.0.0"
  }
}
